let number = parseInt(prompt('choose a number'));
//parseInt convert input in to number

 if (number % 2===0)
//identify if the number is even
  console.log(`${number} is even `);
//print even to console
else
 console.log(`${number} is odd `);
//print odd in to consolel